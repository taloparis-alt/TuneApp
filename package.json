{
  "name": "tuneapp",
  "version": "1.0.0",
  "description": "TuneApp — afinador de guitarra, ukelele y charango con metrónomo",
  "private": true,
  "scripts": {
    "dev": "python devserver.py",
    "sync": "cap sync android",
    "apk": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
